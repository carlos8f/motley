#!/usr/bin/env node
var cmd = require('commander')
  , motley = require('../')

cmd
  .version(require('../package.json').version)
  .description('run a motley project')

cmd.parse(process.argv);

motley(cmd.args[0]).boot();
